# Planning Spec: 添加 hello.txt 示例文件

- Issue: [#2](https://github.com/jelliesem-sketch/deepseek-harness-workspace/issues/2)
- Branch: `looper/planner/2-hello-txt`
- Base: `main`

## Problem

验证 Looper 端到端多 agent 循环（Planner → Reviewer/Fixer → Worker）能否在真实仓库中打通。仓库目前没有任何示例文件，需要一个最小可验证的产物来确认整个工作流从 issue 到 PR 合并的闭环。

## Goals

1. 在仓库根目录创建 `hello.txt`，内容与 issue 要求完全一致。
2. 通过 PR 提交该变更，作为 Looper 多 agent 循环的端到端验证载体。

## Approach

- 在仓库根目录创建 `hello.txt`，内容为：

  ```text
  Hello from Looper multi-agent loop!
  ```

- 变更极小且无外部依赖，仅包含单个文本文件。
- 由 Worker 阶段基于本 spec 完成实际文件创建与 PR 提交。

## Risks

- 风险极低：仅新增一个纯文本文件，无代码、无依赖、无配置变更。
- 唯一需注意的点是内容需与验收标准逐字一致（含结尾换行）。

## Validation

- [ ] `hello.txt` 存在于仓库根目录
- [ ] 内容与 issue 描述完全一致：
  ```text
  Hello from Looper multi-agent loop!
  ```
- [ ] 通过 PR 提交至 `main`
